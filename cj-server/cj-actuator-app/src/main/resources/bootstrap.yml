spring:
  application:
    name: cj-actuator-app
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: www.leoluoxu.com:8848
        namespace: cj-cloud-2x-${spring.profiles.active}
        group: ${spring.profiles.active}
        prefix: ${spring.application.name}
        file-extension: yaml
        username: nacos
        password: nacos
spring:
  application:
    name: cj-gateway-app
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
    web-application-type: reactive
  cloud:
    nacos:
      config:
        server-addr: 10.65.2.101:8848
        namespace: cj-cloud-2x-${spring.profiles.active}
        group: ${spring.profiles.active}
        prefix: ${spring.application.name}
        file-extension: yaml
        username: nacos
        password: Lylw@1995pqzxc!..
    gateway:
      x-forwarded:
        prefix-enabled: false
xss:
  # 开启xss防护
  enable: true
  # 开启sql注入防护
  sql-filter: true
  # 设置xss防护的url拦截路径
  url-patterns: /**
  # 设置xss防护过滤器的优先级，值越小优先级越高
  order: 0
spring:
  application:
    name: cj-web-app
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: cj-cloud-2x-${spring.profiles.active}
        group: ${spring.profiles.active}
        prefix: ${spring.application.name}
        file-extension: yaml
        username: nacos
        password: nacos
minio:
  url: http://192.168.31.154:9000  #对象存储服务的URL
  accessKey: minioadmin #账户
  secretKey: minioadmin #密码
  bucket: tth
spring:
  application:
      name: cj-water-app
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: www.leoluoxu.com:8848
        namespace: cj-cloud-2x-${spring.profiles.active}
        group: ${spring.profiles.active}
        prefix: ${spring.application.name}
        file-extension: yaml
        username: nacos
        password: nacos
minio:
  url: http://www.leoluoxu.com:9000  #对象存储服务的URL
  accessKey: minioadmin #账户
  secretKey: minioadmin #密码
  bucket: tth
does-not-exist: 日期,合计,配水比例,实收
waterResourcePrice: 0.25
dayUseWaterPlanChoseTime: 14
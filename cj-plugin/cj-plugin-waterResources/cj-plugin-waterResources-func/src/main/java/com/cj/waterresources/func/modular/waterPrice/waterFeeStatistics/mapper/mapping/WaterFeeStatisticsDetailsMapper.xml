<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cj.waterresources.func.modular.waterPrice.waterFeeStatistics.mapper.WaterFeeStatisticsDetailsMapper">
    <select id="statistics" resultType="com.cj.waterresources.func.modular.waterPrice.waterFeeStatistics.bean.res.UseWaterTypeStatisticsRes" parameterType="com.cj.waterresources.func.modular.waterPrice.waterFeeStatistics.bean.req.UseWaterTypeStatisticsReq">
        select d1.v,d1.record_time,d2.PARAM_NAME
        from tth.WATER_FEE_STATISTICS_DETAILS d1
        left join tth.TRENDS_TABLE_PARAM d2 on d1.TABLE_HEAD_ID = d2.id
        <where>
            <if test="req.stationList != null and req.stationList.size()>0">
                and d1.TABLE_HEAD_ID in
                <foreach item="item" collection="req.stationList" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="req.startTime !=null and req.startTime !='' and req.endTime != null and req.endTime != ''">
                and TO_CHAR(d1.RECORD_TIME,'YYYY-MM-DD') between #{req.startTime} and #{req.endTime}
            </if>
        </where>
        order by d1.RECORD_TIME
    </select>

</mapper>
